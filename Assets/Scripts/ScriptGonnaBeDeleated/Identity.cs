using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Identity : MonoBehaviour
{
    /*public Charater charater;
    public string identityName;
    public int price;
    [SerializeField] private Transform head;
    [SerializeField] private Transform body;
    [SerializeField] private Transform weapon;
   
    public bool isAlive = true;
    void Start()
    {
        GameObject headTemp = Instantiate(charater.head, head.transform.position, Quaternion.identity);
        GameObject bodyTemp = Instantiate(charater.body, body.transform.position, Quaternion.identity);
        GameObject weaponTemp = Instantiate(charater.weapon, weapon.transform.position, Quaternion.AngleAxis(90,Vector3.forward));
        
        headTemp.transform.parent = head;
        bodyTemp.transform.parent = body;
        weaponTemp.transform.parent = weapon;

        if (weaponTemp.GetComponent<Connect>())
            weaponTemp.GetComponent<Connect>().vatChu =
        //Connect this character with its weapon to use speacial skill, the special skill will be in heroAttack script
        identityName = charater.name;
        price = charater.price;
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void disappear()
    {
        Destroy(this);
        Destroy(head.gameObject);
        Destroy(body.gameObject);
        Destroy(weapon.gameObject);
    }

    */
   
}
